{
  "name": "viebrans",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "single": "npm run build && node build-single.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
